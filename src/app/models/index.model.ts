export class Index {
    indexRow: number = -1;
    indexCol: number = -1;

    constructor(indexRow: number, indexCol: number) {
        this.indexRow = indexRow;
        this.indexCol = indexCol;
    }
}